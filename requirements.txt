# ⚠️ 原生依赖统一锁到成熟版本段：2026-07-11 云端换上最新 wheel 后启动即
# Segmentation fault(本地 Windows 同版本正常，Linux wheel 有雷)。若需升级，逐个放开验证。
streamlit>=1.35.0,<1.60
plotly>=5.20.0
pandas>=2.2.0,<3       # RRG/GEM 用 resample("ME");"ME" 别名需 pandas>=2.2
numpy>=1.26.0,<2.3     # 宏观四象限的因果 z-score / 动量计算
pyarrow>=14.0.0,<21    # 读取提交的行情快照 parquet(lib/snapshot.py)
requests>=2.31.0
# 实时行情：股票/ETF/外汇/加密/期货/指数（含 A股指数 000300.SS 等）
yfinance>=0.2.40
# 智能分析（可选；无 key 时自动降级到规则引擎）
anthropic>=0.40.0

# ── 可选增强（不默认安装）──────────────────────────────────
# akshare：A股北向资金 / 中文财经快讯增强。依赖较重（含需编译的 mini-racer），
# 在部分云环境（如 Streamlit Cloud）可能安装失败，故不放进默认依赖。
# A股指数已用 yfinance(000300.SS) 取数，无 akshare 也有实时行情；
# 本地若需要北向/中文快讯，再单独安装：  pip install akshare
