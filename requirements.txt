# ⚠️ 2026-07-11 云端事故记录：Streamlit Cloud 跑在 Python 3.14 上，最新 Linux wheel
# (pandas3/pyarrow24 组合)启动即 Segmentation fault；而旧版本无 cp314 wheel 会触发源码
# 编译失败(无 cmake)。故按 Python 版本自适应：<3.14 锁成熟版本段(推荐云端设为 3.13)，
# >=3.14 只能放行最新 wheel(有段错误风险——请在 Cloud 设置里把 Python 降到 3.13)。
# streamlit 1.59 把服务器从 Tornado 换成 uvicorn/httptools(原生C扩展)，其 cp314 wheel
# 在云端 Linux 启动即段错误(日志: "Uvicorn server started" 后立刻 Segmentation fault)；
# 本地正常恰因本地是 1.58(Tornado, 纯Python)。锁 <1.59 回到稳定网络栈。
streamlit>=1.35.0,<1.59
plotly>=5.20.0
# 段错误实锤(faulthandler C栈): pandas3 默认 Arrow 字符串 → yfinance 建 DataFrame 时走
# pyarrow25 的 AllocateResizableBuffer → cp314 linux wheel 段错误。锁 pandas<3(2.3.3 有
# cp314 wheel，不走 Arrow 字符串) + pyarrow<25(24.0.0 有 cp314 wheel，本地实测正常)。
pandas>=2.2.0,<3       # RRG/GEM 用 resample("ME") 需 >=2.2
numpy>=1.26.0,<2.3; python_version < "3.14"     # 宏观四象限 z-score / 动量计算
numpy>=2.4; python_version >= "3.14"            # 2.2.x 无 cp314 wheel 会触发源码编译
pyarrow>=14.0.0,<25    # 行情快照 parquet(lib/snapshot.py)
requests>=2.31.0
# 实时行情：股票/ETF/外汇/加密/期货/指数（含 A股指数 000300.SS 等）
yfinance>=0.2.40
# 智能分析（可选；无 key 时自动降级到规则引擎）
anthropic>=0.40.0

# ── 可选增强（不默认安装）──────────────────────────────────
# akshare：A股北向资金 / 中文财经快讯增强。依赖较重（含需编译的 mini-racer），
# 在部分云环境（如 Streamlit Cloud）可能安装失败，故不放进默认依赖。
# A股指数已用 yfinance(000300.SS) 取数，无 akshare 也有实时行情；
# 本地若需要北向/中文快讯，再单独安装：  pip install akshare
